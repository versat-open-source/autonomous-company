# Skill integration

The installed reusable skill is `.agents/skills/versat-mcp/`, retained in its upstream Spanish form. This directory documents the agent template's skill boundary; it is not a second discovery location or a duplicate skill.

Project extensions belong in `AGENTS.md`, the local routing gate and active SDD changes. Imported skill upgrades require a provenance update, offline evals and review. Follow `THIRD_PARTY_NOTICES.md` for source information.
